# 처음 시작하기

빈 저장소에서 **화면에 종목이 뜨기까지** 5분이면 됩니다.
아래 숫자는 전부 깨끗한 클론에서 실제로 측정한 값입니다 (2026-08-16, macOS · Node 24).

> ⚠️ 이 도구는 **투자 참고용**이며 투자 권유가 아닙니다. 최종 판단과 결과는 본인 책임입니다.

---

## 0. 준비물

- **Node.js 20 이상** — `node -v` 로 확인
- 그게 전부입니다. Docker·DB 설치·회원가입·API 키 **모두 불필요**

---

## 1. 내려받고 설치

```bash
git clone https://github.com/YoobinJeon/stock-finder.git
cd stock-finder
npm install
```

## 2. 실행

```bash
npm run dev
```

브라우저가 <http://localhost:5173> 을 자동으로 엽니다.

무슨 일이 일어나는지:

| 포트 | 역할 |
|---|---|
| **5173** | 개발용 화면 (Vite, 코드 고치면 즉시 반영) — **평소 여기로 접속** |
| **4000** | API 서버 + 빌드된 화면. 다른 기기에서 접속할 때 씁니다 |

첫 기동 때 서버가 DB 파일을 만들고 마이그레이션 42개를 자동 적용합니다.
터미널에 `Server running on http://localhost:4000` 이 뜨면 준비 완료입니다.

> DB는 `server/data/` 에 파일로 생깁니다. 지우면 처음 상태로 돌아갑니다.

---

## 3. 데이터 수집 — **여기가 진짜 첫 단계**

설치 직후에는 **화면이 텅 비어 있는 게 정상입니다.** 아직 아무 데이터도 없습니다.

1. 왼쪽 사이드바 맨 아래 **🔄 데이터** 클릭
2. **"시총 상위 200"** 선택 → **수집 시작**
3. 진행률이 `200 / 200` 이 되면 완료

**실측: 44초 · 200종목 전부 성공 · 실패 0건.**
끝나면 종목 200 · 재무 200 · 시세 200 · 점수 200 이 채워집니다.

### 수집 범위 고르기

| 범위 | 대상 | 소요 | 언제 |
|---|---|---|---|
| **시총 상위 200** | 대형주 200 | **~45초** | **처음엔 무조건 이것부터** |
| KOSPI 전체 | KOSPI 전 종목 | 5~8분 | 대형주로 감 잡은 뒤 |
| 전체 시장 | KOSPI+KOSDAQ ~2,800 | **30분~1시간+** | 소형주까지 볼 때. 오래 걸리니 각오하고 |
| 재무만 갱신 | 보유 종목 재무·컨센서스 | 5~7분 | 실적 시즌 |
| 시세만 갱신 | 보유 종목 시세·점수 | 중간 | 장 마감 후 오늘 시세 반영 |
| 점수만 재계산 | 수집 없음 | 1~2분 | 가중치를 바꾼 뒤 |

수집 중에는 다른 수집이 걸리지 않습니다(동시 실행 방지). 진행률은 화면에 실시간으로 뜹니다.

---

## 4. 둘러보기 — 이 순서를 권합니다

### ① 🔍 스크리너 — 가장 먼저 볼 화면

수집한 종목이 점수순으로 나옵니다. 조건을 걸어 좁혀 보세요.

- 왼쪽 **필터**로 PER·ROE·시총·수급·기술 조건 지정
- **전략 칩**을 누르면 미리 만들어 둔 조건이 한 번에 적용됩니다
  (미너비니 SEPA / 오닐 CANSLIM / 드러켄밀러 — 공개 전략의 **근사판**)
- **종목명 클릭** → 상세 패널 (차트·점수 근거·재무·밸류에이션 밴드·공시·뉴스)
- 체크박스로 2~4개 고르고 **⚖️ 선택 비교**
- 우측 상단 **CSV** 로 내보내기

> 점수가 궁금하면 상세 패널의 **점수 분석**을 펼치세요. 가치·퀄리티·성장·모멘텀·신기술
> 각각 몇 점이고 **왜 그 점수인지 실수치 근거**가 붙습니다.

### ② 📊 대시보드

지수·환율·유가 등 매크로, 오늘의 특징주(거래대금/급등/신고가/신규상장), 시장 색깔.
관심 있는 종목은 상세 패널에서 **♡** 를 눌러 담아 두면 여기 위젯에 모입니다.

### ③ 📶 RS 랭킹 · 💰 수급 순위

- **RS 랭킹**: 기간별(1·3·6·12개월) 상대강도 백분위. 섹터 히트맵도 함께.
- **수급 순위**: 외국인·기관·개인 주체별 순매수 상위 종목과 추정 평단·수익률.

### ④ 🧩 테마 · 📦 ETF — 한 번 더 눌러줘야 채워집니다

이 둘은 종목 수집과 별개 데이터라 **처음엔 비어 있습니다.**

- **테마 레이더** → 우측 **"테마 목록 갱신"** 버튼
  → 실측 **28초 · 테마 266개 · 구성종목 6,419건**
- **ETF** → **"스냅샷"** 버튼
  → 실측 **ETF 1,163종 · 차트보드 국면 31종**

한 번 누르면 이후에는 자동 갱신(아래 5번)에 맡길 수 있습니다.

### ⑤ 🎯 전략 관리 → 🧪 백테스트

전략 관리에서 나만의 조건을 저장하고, 백테스트로 그 조건의 과거 수익률을 KOSPI와 비교합니다.

> 백테스트는 **거래비용·세금·슬리피지를 반영하지 않습니다.** 절대 수익률이 아니라
> 전략 간 상대 비교용으로 보세요. 점수 이력이 쌓일수록 결과가 의미 있어집니다.

---

## 5. (선택) 매일 자동으로 갱신하기

수동으로 누르기 귀찮으면 스케줄을 켭니다.

```bash
cp .env.example server/.env
```

`server/.env` 를 열어 필요한 줄의 `#` 을 지웁니다.

```bash
CRON_REFRESH=10 18 * * 1-5          # 평일 18:10 전체 수집
CRON_REFRESH_EARLY=50 15 * * 1-5    # 평일 15:50 마감 직후 시세·점수만
CRON_REFRESH_CATCHUP=5 19-23 * * 1-5 # 컴퓨터가 꺼져 있어 놓친 슬롯 소급 실행
CRON_THEME_SNAPSHOT=50 15 * * 1-5   # 테마 스냅샷
CRON_ETF_SNAPSHOT=50 15 * * 1-5     # ETF 스냅샷
```

**수정 후 서버를 재시작해야 반영됩니다.** (환경변수는 기동 시 한 번만 읽습니다.)

> 18:10인 이유: 네이버 투자자별 수급이 저녁에야 확정되기 때문입니다.
> 스케줄러는 프로세스 안의 타이머라 **컴퓨터가 자면 그 시각을 건너뜁니다** —
> `CRON_REFRESH_CATCHUP` 이 그걸 저녁에 소급해서 채웁니다.

---

## 6. (선택) API 키로 기능 더 켜기

키가 없어도 위 기능은 전부 동작합니다. 아래는 **추가로** 열리는 것들입니다.

| 키 | 열리는 기능 | 발급처 |
|---|---|---|
| `DART_API_KEY` | 공시 수집·이벤트 점수 반영, 실적 캘린더, 분기 실적 3년 백필 | [opendart.fss.or.kr](https://opendart.fss.or.kr) (무료) |
| `KRX_ID` / `KRX_PW` | 투자자 세부 8주체 **공식** 순매수 금액 (없으면 네이버 근사치) | KRX 정보데이터시스템 회원가입 |
| `KIS_APP_KEY` / `KIS_APP_SECRET` | 수정주가 정본 일봉, 신용잔고, 야간선물 | [apiportal.koreainvestment.com](https://apiportal.koreainvestment.com) |

`server/.env` 에 넣고 재시작하면 됩니다. **`.env` 는 git에 올라가지 않습니다.**

---

## 7. (선택) 다른 기기에서 보기

```bash
npm run serve
```

화면을 빌드해서 **:4000 한 포트**로 서빙합니다. 같은 네트워크의 폰·노트북에서
`http://<이 컴퓨터의 IP>:4000` 으로 접속하세요.

> 🔒 **공개 인터넷에 노출한다면 반드시** `server/.env` 에 `ACCESS_PASSWORD` 를 설정하세요.
> 앱 전체에 비밀번호 한 겹이 걸립니다(아이디는 아무거나, 비밀번호만 확인). 미설정이면
> 서버가 기동할 때 경고를 띄웁니다. 인증 실패는 15분에 20회로 제한됩니다.

---

## 자주 막히는 곳

| 증상 | 원인과 해결 |
|---|---|
| **화면은 뜨는데 아무 데이터도 없다** | 정상입니다. 3번 데이터 수집을 아직 안 한 겁니다 |
| **테마·ETF 화면만 비어 있다** | 별도 데이터입니다. 4번 ④의 갱신/스냅샷 버튼을 한 번 누르세요 |
| **실적 캘린더가 비어 있다** | `DART_API_KEY` 가 필요합니다 (6번) |
| **수급 화면 숫자가 대략적이다** | KRX 키 없이는 네이버 근사치를 씁니다 (6번) |
| `Error: listen EADDRINUSE :4000` | 이미 서버가 떠 있습니다. 기존 것을 끄거나 `PORT=4100 npm run dev` |
| **수집이 중간에 멈춘 것 같다** | 데이터 화면의 진행률과 실패 목록을 보세요. 실패 종목은 목록에 남습니다 |
| **점수를 바꿨는데 그대로다** | 전략 관리에서 가중치를 저장한 뒤 **"점수만 재계산"** 을 돌려야 반영됩니다 |
| **DB가 꼬인 것 같다** | 서버를 끄고 `rm -rf server/data` 후 재시작 → 처음부터 다시 수집 |
| **수집 중 서버가 재시작되며 작업이 죽는다** | `npm run dev` 는 코드가 바뀌면 서버를 재시작합니다. 긴 수집(전체 시장) 중에는 코드를 건드리지 마세요 |

### ⚠️ DB는 한 프로세스만 열 수 있습니다

PGlite(파일 DB)의 제약입니다. **서버가 떠 있는 동안** `npm run migrate` 나
`npm run pipeline` 을 따로 돌리면 DB가 잠깁니다. 서버 실행 중 데이터 작업은 화면이나 API로 하세요.

---

## 개발자용

```bash
npm run typecheck   # 서버·클라이언트 타입 검사
npm test            # 단위 테스트 826건
npm run build       # 프로덕션 빌드
```

구조·데이터 흐름·점수 산식·알려진 한계는 [ARCHITECTURE.md](ARCHITECTURE.md) 에 있습니다.

### 검증 이력 (2026-08-16, 깨끗한 클론)

| 항목 | 결과 |
|---|---|
| `npm run typecheck` | 오류 0 |
| `npm test` | 826건 전부 통과 |
| `npm run build` | 성공 (vendor 청크 분리 확인) |
| 서버 기동 | 마이그레이션 42개 자동 적용, 에러 0 |
| `top200` 수집 | 44초 · 200/200 · 실패 0 |
| 화면 API 18종 | 전부 정상 응답 |
| 테마 갱신 | 28초 · 266테마 · 6,419종목 |
| ETF 스냅샷 | 1,163 ETF · 국면 31종 |

---

## 마지막으로

이 저장소는 **공유 목적의 스냅샷**이며 유지보수하지 않습니다.
외부 사이트에서 데이터를 긁어오므로 **소스 쪽이 바뀌면 수집이 깨질 수 있습니다.**
포크해서 고쳐 쓰시면 됩니다.

그리고 다시 한 번 — 여기 나오는 점수·시그널·백테스트는 전부 **참고 자료**입니다.
투자 판단과 그 결과는 전적으로 본인 책임입니다.
